# Changelog

## Unreleased

- Bootstrapped the `lx` Laravel Zero application and packaging workflow.
- Added scaffold services, Twig-based stubs, and namespace resolution.
- Implemented `make:service`, `make:repository`, `make:dto`, and `make:action`.
- Added `.lxconfig.yml` loading with scaffold defaults and a sample config file.
- Added CI quality/build jobs plus Laravel 10 and 11 compatibility smoke tests.
